---
title: "Homework 7"
author: "Shelby, Seana, and Greg"
date: "2023-03-22"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

```{r}
library(classdata)
library(ggplot2)
library(tidyverse)
data(happy, package="productplots")
head(happy)
```

Loading extra data into R

```{r}
happy.new <- readRDS("data/HAPPY.rds")
str(happy.new)
```


1.Data cleaning: the values “IAP”, “DK” and “NA” all encode missing values. Replace all of these instances by the value NA. Show with bar/histogram

```{r, error=TRUE}
str(HAPPY)
happy.new <- replace(happy.new, HAPPY == "IAP", NA)
happy.new <- replace(happy.new, HAPPY == "DK", NA)
happy.new <- replace(happy.new, HAPPY == "NA", NA)
happy.new <- replace(happy.new, SEX == "IAP", NA)
happy.new <- replace(happy.new, SEX == "DK", NA)
happy.new <- replace(happy.new, SEX == "NA", NA)
happy.new <- replace(happy.new, MARITAL == "IAP", NA)
happy.new <- replace(happy.new, MARITAL == "DK", NA)
happy.new <- replace(happy.new, MARITAL == "NA", NA)
happy.new <- replace(happy.new, DEGREE == "IAP", NA)
happy.new <- replace(happy.new, DEGREE == "DK", NA)
happy.new <- replace(happy.new, DEGREE == "NA", NA)
happy.new <- replace(happy.new, FINRELA == "IAP", NA)
happy.new <- replace(happy.new, FINRELA == "DK", NA)
happy.new <- replace(happy.new, FINRELA == "NA", NA)
happy.new <- replace(happy.new, HEALTH == "IAP", NA)
happy.new <- replace(happy.new, HEALTH == "DK", NA)
happy.new <- replace(happy.new, HEALTH == "NA", NA)
happy.new <- replace(happy.new, PARTYID == "IAP", NA)
happy.new <- replace(happy.new, PARTYID == "DK", NA)
happy.new <- replace(happy.new, PARTYID == "NA", NA)
happy.new <- replace(happy.new, POLVIEWS == "IAP", NA)
happy.new <- replace(happy.new, POLVIEWS == "DK", NA)
happy.new <- replace(happy.new, POLVIEWS == "NA", NA)

str(happy.new)
```

```{r}
ggplot(happy.new, aes(x = HAPPY)) + geom_bar()
ggplot(happy.new, aes(x = SEX)) + geom_bar()
ggplot(happy.new, aes(x = MARITAL)) + geom_bar()
ggplot(happy.new, aes(x = DEGREE)) + geom_bar()
ggplot(happy.new, aes(x = FINRELA)) + geom_bar()
ggplot(happy.new, aes(x = HEALTH)) + geom_bar()
ggplot(happy.new, aes(x = PARTYID)) + geom_bar()
ggplot(happy.new, aes(x = POLVIEWS)) + geom_bar()


```

2. Check the type of the variable and cast into the right type (factor variable for categorical variables). For age, change “89 OR OLDER” to 89 and assume the variable should be numeric.Show with bar/histogram
```{r}
happy.new$HAPPY  <- as.factor(happy.new$HAPPY)
happy.new$SEX    <- as.factor(happy.new$SEX)
happy.new$AGE    <- as.numeric(replace(happy.new$AGE, happy.new$AGE == "89 OR OLDER", 89))
  happy.new$AGE  <- as.factor(happy.new$AGE)
happy.new$MARITAL<- as.factor(happy.new$MARITAL)
happy.new$DEGREE <- as.factor(happy.new$DEGREE)
happy.new$FINRELA<- as.factor(happy.new$FINRELA)
happy.new$HEALTH <- as.factor(happy.new$HEALTH)
happy.new$PARTYID<- as.factor(happy.new$PARTYID)
happy.new$POLVIEWS<- as.factor(happy.new$POLVIEWS)
```


3. Bring all levels of factors into a sensible order. For marital you could e.g. order the levels according to average age. Show with bar/histogram

```{r}

happy.new <- happy.new %>%
  mutate(DEGREE=fct_relevel(DEGREE, "LT HIGH SCHOOL", "HIGH SCHOOL", "JUNIOR COLLEGE", "BACHELOR", "GRADUATE", "NA's"))

happy.new <- happy.new %>%
  mutate(MARITAL=fct_relevel(MARITAL, "MARRIED", "NEVER MARRIED", "SEPARATED", "DIVORCED", "WIDOWED", "NA's"))

happy.new <- happy.new %>%
  mutate(FINRELA=fct_relevel(FINRELA, "FAR BELOW AVERAGE", "BELOW AVERAGE", "AVERAGE", "ABOVE AVERAGE", "FAR ABOVE AVERAGE", "NA's"))

happy.new <- happy.new %>%
  mutate(HEALTH=fct_relevel(HEALTH, "POOR", "FAIR", "GOOD", "EXCELLENT", "NA's"))

happy.new <- happy.new %>%
  mutate(PARTYID=fct_relevel(PARTYID, "STRONG REPUBLICAN", "NOT STR REPUBLICAN", "IND,NEAR REP", "INDEPENDENT", "IND,NEAR DEM", "NOT STR DEMOCRAT", "STRONG DEMOCRAT", "OTHER PARTY", "NA's"))

happy.new <- happy.new %>%
  mutate(POLVIEWS=fct_relevel(POLVIEWS, "EXTRMLY CONSERVATIVE", "CONSERVATIVE", "SLGHTLY CONSERVATIVE", "MODERATE", "SLIGHTLY LIBERAL", "LIBERAL", "EXTREMELY LIBERAL", "NA's"))

ggplot(happy.new, aes(x = HAPPY)) + geom_bar()
ggplot(happy.new, aes(x = SEX)) + geom_bar()
ggplot(happy.new, aes(x = MARITAL)) + geom_bar()
ggplot(happy.new, aes(x = DEGREE)) + geom_bar()
ggplot(happy.new, aes(x = FINRELA)) + geom_bar()
ggplot(happy.new, aes(x = HEALTH)) + geom_bar()
ggplot(happy.new, aes(x = PARTYID)) + geom_bar()
ggplot(happy.new, aes(x = POLVIEWS)) + geom_bar()

```

4.Investigate the relationship between happiness and two other variables in the data. Find a visualization that captures the relationship and write a paragraph to describe it.

Each member should study a different question (choose different variables to investigate).

Upload your answers and the dataset to the repository. Discuss and refine answers as a team.

Shelby-
Seana-
Greg-
